{% extends 'admin/base_admin.html.twig' %}

{% block title %}Nouveau Combat{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center reveal-item" data-reveal-target="item">
        <div class="col-md-8">
            <div class="card card-custom border-primary">
                <div class="card-header bg-transparent border-bottom border-secondary">
                    <h3 class="card-title text-primary mb-0">Créer un nouveau combat</h3>
                </div>
                <div class="card-body">
                    {{ include('admin/fight/_form.html.twig') }}
                </div>
                <div class="card-footer bg-transparent border-top border-secondary text-end">
                    <a href="{{ path('app_fight_index') }}" class="btn btn-outline-light">Annuler</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}